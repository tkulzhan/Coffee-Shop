import { SafeAreaView } from "react-native";
import App from "../components/App";

function Home() {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <App />
    </SafeAreaView>
  );
}

export default Home;
